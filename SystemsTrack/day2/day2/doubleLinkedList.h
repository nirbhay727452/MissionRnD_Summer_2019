#include<stdio.h>
#include<stdlib.h>
/*
struct node{
	int value;
	struct node*next;
	struct node* prev;
};

struct node*create_node(int num){
	struct node*temp = (struct node*)malloc(sizeof(struct node));
	temp->value = num;
	temp->next = NULL;
	temp->prev = NULL;
	return temp;
}
*/